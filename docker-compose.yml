version: '3.8'

services:
  # PostgreSQL Database
  postgres:
    image: postgres:16-alpine
    container_name: abac-postgres
    environment:
      POSTGRES_DB: abacdb
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: Rin25052001
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
      - ./src/main/resources/sql:/docker-entrypoint-initdb.d
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres"]
      interval: 10s
      timeout: 5s
      retries: 5
    networks:
      - abac-network

  # Spring Boot Application
#  app:
#    build:
#      context: .
#      dockerfile: Dockerfile
#    container_name: abac-app
#    environment:
#      SPRING_DATASOURCE_URL: jdbc:postgresql://postgres:5432/abacdb
#      SPRING_DATASOURCE_USERNAME: postgres
#      SPRING_DATASOURCE_PASSWORD: Rin25052001
#      SPRING_DATASOURCE_DRIVER_CLASS_NAME: org.postgresql.Driver
#      SPRING_JPA_DATABASE_PLATFORM: org.hibernate.dialect.PostgreSQLDialect
#      SPRING_JPA_HIBERNATE_DDL_AUTO: update
#      SPRING_JPA_SHOW_SQL: "false"
#      SERVER_PORT: 8081
#    ports:
#      - "8081:8081"
#    depends_on:
#      postgres:
#        condition: service_healthy
#    healthcheck:
#      test: ["CMD", "wget", "--no-verbose", "--tries=1", "--spider", "http://localhost:8081/api/public"]
#      interval: 30s
#      timeout: 10s
#      retries: 3
#      start_period: 40s
#    networks:
#      - abac-network
#    restart: unless-stopped

volumes:
  postgres_data:
    driver: local

networks:
  abac-network:
    driver: bridge

